# Replication Package Manifest

- Package directory: `replication_package/`
- Scope: shareable processed inputs, frozen model outputs, robustness summaries, appendix audit notes, and wrapper scripts for package-local reruns
- External archive hash record: the SHA-256 for `replication_package.zip` is maintained in the repository root at `appendix/replication_archive_manifest.md`